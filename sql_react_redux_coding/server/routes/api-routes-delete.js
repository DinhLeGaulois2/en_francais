const Sequelize = require('sequelize');
var sequelize = models.sequelize

const db = require("../models");

module.exports = function (app) {
    app.delete("api/add/PPPPPPPPPPPPPPPPP/:id", (req, res, next) => {

    })
}