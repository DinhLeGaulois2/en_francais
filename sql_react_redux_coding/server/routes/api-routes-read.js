const Sequelize = require('sequelize');

const db = require("../models");


module.exports = function (app) {
    app.get("/api/get/PPPPPPPPP/:id", (req, res, next) => {
        
    }) // get 'booking' with 'id'
}